var datasetDefs = require('../dataset-defs')
var assert = require('assert')

describe('datasetDefs - ', function() {

	it('dataset has correctly set dataDir param', function() {
		
		var dataset = datasetDefs.dataset()

		console.log(dataset)

	})
})
